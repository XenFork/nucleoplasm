package union.xenfork.nucleoplasm.chemistry.items.extendItem.items;

import union.xenfork.nucleoplasm.chemistry.items.extendItem.BaseItem;

public class Item51to60 {
    public static class SbItem extends BaseItem {

        public SbItem(Settings settings) {
            super(settings);
        }
    }

    public static class TeItem extends BaseItem {

        public TeItem(Settings settings) {
            super(settings);
        }
    }

    public static class IItem extends BaseItem {

        public IItem(Settings settings) {
            super(settings);
        }
    }

    public static class XeItem extends BaseItem {

        public XeItem(Settings settings) {
            super(settings);
        }
    }

    public static class CsItem extends BaseItem {

        public CsItem(Settings settings) {
            super(settings);
        }
    }

    public static class BaItem extends BaseItem {

        public BaItem(Settings settings) {
            super(settings);
        }
    }

    public static class LaItem extends BaseItem {

        public LaItem(Settings settings) {
            super(settings);
        }
    }

    public static class CeItem extends BaseItem {

        public CeItem(Settings settings) {
            super(settings);
        }
    }

    public static class PrItem extends BaseItem {

        public PrItem(Settings settings) {
            super(settings);
        }
    }

    public static class NdItem extends BaseItem {

        public NdItem(Settings settings) {
            super(settings);
        }
    }
}
