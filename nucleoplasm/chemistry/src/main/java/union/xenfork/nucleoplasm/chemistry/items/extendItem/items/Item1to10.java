package union.xenfork.nucleoplasm.chemistry.items.extendItem.items;

import union.xenfork.nucleoplasm.chemistry.items.extendItem.BaseItem;

public class Item1to10 {
    public static class HItem extends BaseItem {
        public HItem(Settings settings) {
            super(settings);
        }
    }

    public static class HeItem extends BaseItem {
        public HeItem(Settings settings) {
            super(settings);
        }
    }

    public static class LiItem extends BaseItem {
        public LiItem(Settings settings) {
            super(settings);
        }
    }

    public static class BeItem extends BaseItem {
        public BeItem(Settings settings) {
            super(settings);
        }
    }

    public static class BItem extends BaseItem {
        public BItem(Settings settings) {
            super(settings);
        }
    }

    public static class CItem extends BaseItem {
        public CItem(Settings settings) {
            super(settings);
        }
    }

    public static class NItem extends BaseItem {
        public NItem(Settings settings) {
            super(settings);
        }
    }

    public static class OItem extends BaseItem {
        public OItem(Settings settings) {
            super(settings);
        }
    }

    public static class FItem extends BaseItem {
        public FItem(Settings settings) {
            super(settings);
        }
    }

    public static class NeItem extends BaseItem {
        public NeItem(Settings settings) {
            super(settings);
        }
    }
}
