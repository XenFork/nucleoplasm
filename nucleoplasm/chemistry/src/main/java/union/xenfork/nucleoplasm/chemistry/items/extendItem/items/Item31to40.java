package union.xenfork.nucleoplasm.chemistry.items.extendItem.items;

import union.xenfork.nucleoplasm.chemistry.items.extendItem.BaseItem;

public class Item31to40 {
    public static class AsItem extends BaseItem {
        public AsItem(Settings settings) {
            super(settings);
        }
    }

    public static class BrItem extends BaseItem {
        public BrItem(Settings settings) {
            super(settings);
        }
    }

    public static class GaItem extends BaseItem {
        public GaItem(Settings settings) {
            super(settings);
        }
    }

    public static class GeItem extends BaseItem {
        public GeItem(Settings settings) {
            super(settings);
        }
    }

    public static class KrItem extends BaseItem {
        public KrItem(Settings settings) {
            super(settings);
        }
    }

    public static class RbItem extends BaseItem {
        public RbItem(Settings settings) {
            super(settings);
        }
    }

    public static class SeItem extends BaseItem {
        public SeItem(Settings settings) {
            super(settings);
        }
    }

    public static class SrItem extends BaseItem {
        public SrItem(Settings settings) {
            super(settings);
        }
    }

    public static class YItem extends BaseItem {
        public YItem(Settings settings) {
            super(settings);
        }
    }

    public static class ZrItem extends BaseItem {
        public ZrItem(Settings settings) {
            super(settings);
        }
    }
}
