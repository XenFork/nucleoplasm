package union.xenfork.nucleoplasm.chemistry.items.extendItem.items;

import union.xenfork.nucleoplasm.chemistry.items.extendItem.BaseItem;

public class Item21to30 {
    public static class CoItem extends BaseItem {
        public CoItem(Settings settings) {
            super(settings);
        }
    }

    public static class CrItem extends BaseItem {
        public CrItem(Settings settings) {
            super(settings);
        }
    }

    public static class CuItem extends BaseItem {
        public CuItem(Settings settings) {
            super(settings);
        }
    }

    public static class FeItem extends BaseItem {
        public FeItem(Settings settings) {
            super(settings);
        }
    }

    public static class MnItem extends BaseItem {
        public MnItem(Settings settings) {
            super(settings);
        }
    }

    public static class NiItem extends BaseItem {
        public NiItem(Settings settings) {
            super(settings);
        }
    }

    public static class ScItem extends BaseItem {
        public ScItem(Settings settings) {
            super(settings);
        }
    }

    public static class TiItem extends BaseItem {
        public TiItem(Settings settings) {
            super(settings);
        }
    }

    public static class VItem extends BaseItem {
        public VItem(Settings settings) {
            super(settings);
        }
    }

    public static class ZnItem extends BaseItem {
        public ZnItem(Settings settings) {
            super(settings);
        }
    }

}
