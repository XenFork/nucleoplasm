package union.xenfork.nucleoplasm.chemistry.items.extendItem.items;

import union.xenfork.nucleoplasm.chemistry.items.extendItem.BaseItem;

public class Item11to20 {
    public static class AlItem extends BaseItem {
        public AlItem(Settings settings) {
            super(settings);
        }
    }

    public static class ArItem extends BaseItem {
        public ArItem(Settings settings) {
            super(settings);
        }
    }

    public static class CaItem extends BaseItem {
        public CaItem(Settings settings) {
            super(settings);
        }
    }

    public static class ClItem extends BaseItem {
        public ClItem(Settings settings) {
            super(settings);
        }
    }

    public static class KItem extends BaseItem {
        public KItem(Settings settings) {
            super(settings);
        }
    }

    public static class MgItem extends BaseItem {
        public MgItem(Settings settings) {
            super(settings);
        }
    }

    public static class NaItem extends BaseItem {
        public NaItem(Settings settings) {
            super(settings);
        }
    }

    public static class PItem extends BaseItem {
        public PItem(Settings settings) {
            super(settings);
        }
    }

    public static class SiItem extends BaseItem {
        public SiItem(Settings settings) {
            super(settings);
        }
    }

    public static class SItem extends BaseItem {
        public SItem(Settings settings) {
            super(settings);
        }
    }

}
