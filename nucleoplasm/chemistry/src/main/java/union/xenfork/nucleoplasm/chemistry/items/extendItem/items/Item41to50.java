package union.xenfork.nucleoplasm.chemistry.items.extendItem.items;

import union.xenfork.nucleoplasm.chemistry.items.extendItem.BaseItem;

public class Item41to50 {
    public static class AgItem extends BaseItem {
        public AgItem(Settings settings) {
            super(settings);
        }
    }

    public static class CdItem extends BaseItem {
        public CdItem(Settings settings) {
            super(settings);
        }
    }

    public static class InItem extends BaseItem {
        public InItem(Settings settings) {
            super(settings);
        }
    }

    public static class MoItem extends BaseItem {
        public MoItem(Settings settings) {
            super(settings);
        }
    }

    public static class NbItem extends BaseItem {
        public NbItem(Settings settings) {
            super(settings);
        }
    }

    public static class PdItem extends BaseItem {
        public PdItem(Settings settings) {
            super(settings);
        }
    }

    public static class RhItem extends BaseItem {
        public RhItem(Settings settings) {
            super(settings);
        }
    }

    public static class RuItem extends BaseItem {
        public RuItem(Settings settings) {
            super(settings);
        }
    }

    public static class SnItem extends BaseItem {
        public SnItem(Settings settings) {
            super(settings);
        }
    }

    public static class TcItem extends BaseItem {
        public TcItem(Settings settings) {
            super(settings);
        }
    }
}
