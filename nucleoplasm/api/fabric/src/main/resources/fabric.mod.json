{
  "schemaVersion": 1,
  "id": "${modid}",
  "version": "${version}",
  "name": "${name}",
  "description": "",
  "authors": [
    "${authors}"
  ],
  "contact": {},

  "license": "${license}",
  "icon": "assets/${modid}/icon.png",

  "environment": "*",
  "entrypoints": {
    "server": [
      "union.xenfork.nucleoplasm.api.fabric.NucleoplasmServer"
    ],
    "main": [
      "union.xenfork.nucleoplasm.api.fabric.Nucleoplasm"
    ]
  },
  "mixins": [
    "${modid}.mixins.json",
    "${modid}-common.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "minecraft": ">=${minecraft_version}",
    "fabric-api": "*"
  }
}
